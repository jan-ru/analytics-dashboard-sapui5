sap.ui.define([
  "sap/ui/core/mvc/Controller"
], function (Controller) {
  "use strict";

  return Controller.extend("analytics.dashboard.controller.Ui5Components", {

    /**
     * Called when the controller is instantiated
     * @public
     */
    onInit: function () {
      // Initialize controller
    }

  });
});
