sap.ui.define([
  "sap/ui/core/mvc/Controller"
], function (Controller) {
  "use strict";

  return Controller.extend("analytics.dashboard.controller.SapColors", {

    /**
     * Called when the controller is instantiated
     * @public
     */
    onInit: function () {
      // Initialize controller
    }

  });
});
